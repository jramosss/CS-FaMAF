#include <stdio.h>
#include <stdbool.h>
bool es_primo (int N, int i,bool res) {
	if (N<0)
		printf("Error, Inserte un valor positivo");
	}
	else {
	while (res && i < N) {
	res = res && (!(N % i = 0))
	i = i + 1;
	}
return res;
}

int main () {
	bool res;
	
	if res {
		printf("El numero ingresado es primo");
	else {
		printf("El numero ingresado no es primo");
}
}
return 0;
} 
	
