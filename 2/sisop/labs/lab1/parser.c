#include<stdio.h>
#include<stdlib.h>
#include<assert.h>
#include "parser.h"
/*
Parser parser_new(FILE *input) {
    assert(input != NULL);
    Parser par = (Parser)calloc(1,sizeof(struct parser_s));
}
*/